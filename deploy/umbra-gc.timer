# Ежедневный запуск GC осиротевших блобов.
# Кладётся в /etc/systemd/system/umbra-gc.timer
#   systemctl enable --now umbra-gc.timer
[Unit]
Description=Ежедневный GC осиротевших блобов Umbra

[Timer]
OnCalendar=*-*-* 04:30:00
RandomizedDelaySec=1h
Persistent=true

[Install]
WantedBy=timers.target
