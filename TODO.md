# Umbra — текущее состояние

Этот файл — один актуальный срез: только то, что не сделано, и то, что нужно
проверить перед выпуском. История сделанного по версиям — в
[`docs/history/CHANGES.md`](docs/history/CHANGES.md); план функций и статусы — в
[`PRODUCT_ROADMAP.md`](PRODUCT_ROADMAP.md). Не добавляйте сюда разделы «Сделано в X».

## Блокеры релиза

- [x] Применить миграции `018_message_reads.sql` и `019_chat_reads.sql` на боевой базе —
      проверено 14 сентября 2026: таблицы `message_reads` и `chat_reads` в проде.
- [x] Задать переменную репозитория `UMBRA_SERVER_URL` с HTTPS-адресом: сборка APK
      теперь падает на `http://` без явного `allowInsecureServerUrl` —
      задано `https://game.agentwill.ru:8443` (сертификат действует до 15.11.2026).
- [x] Выпустить сервер с алиасом поля `payload` РАНЬШЕ клиента: новые сборки
      шлют `payload` и `ciphertext` одновременно, но читают `payload` первым —
      алиас работал на сервере с 0.18.3; 14 сентября 2026 прод переведён на
      0.18.5 (сервер и клиент).

## Проверки перед production

- [ ] `assembleDebug`, `assembleRelease`, `lintDebug`, Room migration и instrumented tests.
- [ ] Группа на трёх устройствах: счётчик прочтений и скрытие статуса.
- [ ] Предпросмотр ссылки: сайт без og-тегов, редирект, локальный адрес (отказ).
- [ ] Отправка в авиарежиме с закрытием приложения — сообщение должно уйти само.
- [ ] Старый клиент (только `ciphertext`) против нового сервера и наоборот.
- [ ] HTTP 401/403/429/5xx и отключение WebSocket при работающем REST.
- [ ] Android 8/9, 12, 14/15; телефон, foldable, планшет; 60/90/120 Гц, reduced motion,
      TalkBack, крупный шрифт.
- [ ] Два аккаунта на двух устройствах: общий hotspot, отдельный Wi-Fi, VPN.

## Техдолг

- [ ] Разбить `ChatRepository.kt` (~2300 строк) на outbox / историю / медиа / прочтения
      с общим фасадом (переносить только с запущенной сборкой: `private` в Kotlin
      ограничен файлом).
- [x] `ChatView.kt` разбит: осталось 875 строк (лента, топбар, пустое состояние),
      остальное — `ChatComposerSection.kt`, `ChatMessageRow.kt`, `ChatDialogs.kt`.
- [x] Общий conformance-тест для `MemoryStore` и `PostgresStore`:
      `internal/store/conformance_test.go` (один контракт на оба хранилища).
- [ ] Удалить алиас `ciphertext` из API после обновления всех устройств (цель — 0.19).
- [ ] Инвайт-коды вместо ручной выдачи владельцем: TTL, лимит использований,
      журнал выдачи (нужна миграция и эндпоинты).
- [ ] Push «печатает…» и счётчики прочтений только через WebSocket (убрать опрос).

## Продуктовая очередь

1. Альбомы — несколько фото одним сообщением (новый тип конверта).
2. Исчезающие сообщения — поле `expiresAt` есть, нужны UI и серверная очистка.
3. Закреплённые сообщения и чаты, мультивыбор для пересылки.
4. Авторы реакций (сервер отдаёт список, а не только счётчики).
5. Редактор фото перед отправкой, транскрипция голосовых.

«Задачи (Tasks)» отложены сознательно: самая дорогая фича для семейного
мессенджера, берём после стабилизации ядра.

## Отдельные проекты

- Сквозное шифрование — меняет протокол и требует аудита. Первый шаг — документ
  с моделью угроз и выбором схемы (per-device ключи, верификация контактов).
- Групповые звонки больше трёх участников — mesh 4×4 упирается в мобильные сети:
  сначала понижение битрейта, затем оценка SFU.
- Возобновляемая загрузка крупных файлов — chunked upload с резюмированием по `upload_id`.
