# Umbra — актуальное состояние после 0.16.7

## Готово

- Надёжная отправка независимо от успешной загрузки истории.
- Tolerant-разбор ISO времени `Z` и смещений вроде `+03:00`.
- Диагностический журнал без содержимого сообщений.
- Точные состояния REST и WebSocket.
- Future UI, Motion Engine и Spatial Media.
- Alien Interface с тремя интенсивностями, таблицей токенов и общей библиотекой `AlienVisuals.kt`.
- Source-aware медиапереход, Predictive Back, swipe-dismiss и pan/zoom arbitration.
- Телефонная и планшетная компоновки.

## Обязательно проверить перед production

- `assembleDebug`, `assembleRelease`, `lintDebug`.
- Room migration и instrumented tests.
- Два аккаунта на двух устройствах через общий hotspot, отдельный Wi-Fi и VPN.
- HTTP 401/403/429/5xx и отключение WebSocket при работающем REST.
- Android 8/9, 12, 14/15; телефон, foldable и планшет.
- Плавность 60/90/120 Гц, reduced motion, TalkBack и крупный шрифт.

## Следующий продуктовый этап

- Umbra 0.17: Tasks — модель задач, создание из сообщения, раздел «Сегодня», offline queue и синхронизация.

## Сделано в 0.17.1

- «Печатает…» в шапке чата (личные и группы).
- Отмена загрузки вложения из индикатора прогресса.
- Черновики сообщений по чатам.
- Галерея «Медиа и файлы» в меню чата.

## Запланировано, но требует отдельных версий

Эти пункты меняют схему данных или протокол, поэтому их нельзя
делать без запуска сборки, миграций Room и тестов на устройстве:

- Альбомы (несколько фото одним сообщением) — новый тип конверта.
- Кто поставил реакцию — сервер должен отдавать авторов, а не только счётчики.
- Исчезающие сообщения — поле expiresAt есть, нужны UI и серверная очистка.
- Закреплённые сообщения и чаты, выделение нескольких сообщений для пересылки.
- Фоновая догрузка вложений через WorkManager и автозагрузка по типу сети.
- Редактор фото перед отправкой, транскрипция голосовых.
- Сквозное шифрование — отдельный проект с аудитом, а не одна правка.

## Сделано в 0.17.2

- Повтор всего неотправленного одной кнопкой.
- Автозагрузка вложений по типу сети + ручная загрузка превью.
- Размер и очистка кэша медиа в настройках.

## Осталось из блока «довести до ума»

- Push «печатает…» и presence через WebSocket вместо опроса (нужен серверный hub-broadcast).
- Свайп-ответ, предпросмотр ссылок, «Прочитано» в личных чатах.


## Сделано в 0.17.3

- Push статуса «в сети / был(а)» по WebSocket, опрос — только страховка.

## Осталось из блока «доводка»

- «Прочитано» в личных чатах (нужна таблица прочтений и миграция).
- Предпросмотр ссылок в сообщениях (нужен серверный резолвер, чтобы не светить IP).
- Возобновляемая загрузка крупных файлов и WorkManager для очереди отправки.


## Сделано в 0.17.4

- «Прочитано» в личных чатах: курсор чтения на сервере, push автору, две галочки в UI.

## Дальше

- Прочтения в группах (курсор на каждого участника + список «кто прочитал»).
- Предпросмотр ссылок через серверный резолвер (не светить IP клиента).
- Возобновляемая загрузка крупных файлов и WorkManager для очереди отправки.
