<resources>
    <style name="Theme.Umbra" parent="android:Theme.Material.NoActionBar">
        <!-- Прозрачные системные панели: контент рисуется под ними, отступы
             добавляет Scaffold. Цвет окна совпадает с фоном экранов Compose. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">#091120</item>
    </style>
</resources>
