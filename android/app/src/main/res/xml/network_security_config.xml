<?xml version="1.0" encoding="utf-8"?>
<!--
  Сетевая политика Umbra:
  - по умолчанию cleartext (HTTP) запрещён — прод работает только по HTTPS;
  - исключения — dev-адреса: эмулятор → хост (10.0.2.2), localhost и IP
    тестового стенда (http://194.226.126.253:8081). Удалить стенд-IP после
    переезда на HTTPS-домен (см. di/AppContainer.kt).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">194.226.126.253</domain>
    </domain-config>
</network-security-config>
