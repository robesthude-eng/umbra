<?xml version="1.0" encoding="utf-8"?>
<!--
  Сетевая политика Umbra:
  - по умолчанию cleartext (HTTP) запрещён — прод работает только по HTTPS;
  - dev-адреса эмулятора/localhost исключены, чтобы http://10.0.2.2:8080 работал
    при локальной разработке (см. di/AppContainer.kt).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
